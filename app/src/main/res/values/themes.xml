<resources xmlns:android="http://schemas.android.com/apk/res/android" xmlns:tools="http://schemas.android.com/tools">
<!-- Base application theme. -->
    <style name="Base.Theme.Appxemphim" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/bg_toolbar</item>
        <item name="android:background">@color/white</item>
        <item name="android:textColor">@color/black</item>
        <item name="tabSelectedTextColor">@color/black</item>
        <item name="tabIndicator">@color/black</item>
        <item name="btn_drawable">@color/black</item>
    </style>
    <style name="SearchViewStyle" parent="Widget.AppCompat.SearchView">
        <!-- Thiết lập nền -->
        <item name="android:background">@color/gray</item>
    </style>
    <style name="Theme.Appxemphim" parent="Base.Theme.Appxemphim" >
        <item name="android:popupMenuStyle">@style/MyPopupMenuStyle</item>
    </style>

    <style name="Widget.Appxemphim.Chip.Filter" parent="Widget.MaterialComponents.Chip.Filter">
        <item name="materialThemeOverlay">@style/ThemeOverlay.Appxemphim.Chip.Filter</item>
    </style>
    <!-- Disabling elevation overlays because when chip's default background/surface
         composite are combined with elevation overlays in a highly elevated dialog,
         the resulting color becomes too light and causes contrast
         issues. -->
    <style name="ThemeOverlay.Appxemphim.Chip.Filter" parent="">
        <item name="elevationOverlayEnabled">false</item>
    </style>
    <style name="MyPopupMenuStyle" parent="@style/Widget.AppCompat.PopupMenu">
        <item name="android:popupBackground">@color/background_popup</item>
    </style>

</resources>