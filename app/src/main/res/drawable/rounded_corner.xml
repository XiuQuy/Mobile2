<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="5dp" /> <!-- Điều chỉnh giá trị radius để bo cong góc -->
    <solid android:color="@android:color/transparent" />
</shape>
